import React from "react";

import { Box } from "grommet";

import Header from "../components/Header/Header.component";

const Home = () => {
  return (
    <Box margin={{ left: "25rem", right: "25rem", top: "1.5rem" }}>
      <Header />
    </Box>
  );
};

export default Home;
